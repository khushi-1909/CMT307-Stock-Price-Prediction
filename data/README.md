to be included for this:
- raw data is downloaded programmatically, not stored manually
- default ticker is MSFT
- date range used in the project
- whether processed CSVs are saved locally or regenerated each run

any CSV files for the dataset that is being saved